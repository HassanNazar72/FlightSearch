# Base URL of the Django API. Leave empty in development (Vite proxies /api to localhost:8000).
# In production set it to the deployed backend, e.g. https://flight-search-api.onrender.com
VITE_API_URL=
